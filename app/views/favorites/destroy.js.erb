
var card = $("#yelp_place_<%= @favorite.yelp_id %>");

// Fade out the card and remove it from the DOM
card.fadeOut(500, function() {
  card.remove();
});

// Update the heart icon within the clicked card
$(".fa-heart", card).removeClass("fa-solid").addClass("fa-regular");

