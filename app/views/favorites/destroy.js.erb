
$("#yelp_place_<%= @favorite.yelp_id %>").fadeOut(500, function() {
  var container = $(this);
  var heartIcon = container.find(".fa-heart");
  heartIcon.removeClass("fa-solid").addClass("fa-regular");
  container.remove();
});
